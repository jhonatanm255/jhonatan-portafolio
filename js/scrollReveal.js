
// Inicializar ScrollReveal
// LOGO
ScrollReveal().reveal('.logo', {
  origin: 'left',       // Aparece desde la izquierda
  distance: '30px',    // Se desplaza 100px
  duration: 1000,       // Dura 1 segundo
  delay: 100,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.3 
});

// // MENU
// ScrollReveal().reveal('.menu', {
//   origin: 'top',       // Aparece desde la izquierda
//   distance: '50px',    // Se desplaza 100px
//   duration: 1000,       // Dura 1 segundo
//   delay: 100,           // Retraso de 0.3 segundos
//   scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
//   opacity: 0,           // Comienza invisible
//   reset: false,         // Solo se anima la primera vez
//   easing: 'ease-in-out',  // Animación suave
//   viewFactor: 0.3 
// });

// MAIN
ScrollReveal().reveal('.contenedor-texto', {
  origin: 'top',       // Aparece desde la izquierda
  distance: '50px',    // Se desplaza 100px
  duration: 1000,       // Dura 1 segundo
  delay: 150,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.3 
});

// ABOUT ME
ScrollReveal().reveal('.about-me', {
  origin: 'bottom',       // Aparece desde la izquierda
  distance: '50px',    // Se desplaza 100px
  duration: 1000,       // Dura 1 segundo
  delay: 50,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.3 
});

// IMG
ScrollReveal().reveal('.img', {
  origin: 'top',       // Aparece desde la izquierda
  distance: '20px',    // Se desplaza 100px
  duration: 600,       // Dura 1 segundo
  delay: 250,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.3 
});

// SOFT SKILLS
ScrollReveal().reveal('.softskills', {
  origin: 'bottom',       // Aparece desde la izquierda
  distance: '50px',    // Se desplaza 100px
  duration: 800,       // Dura 1 segundo
  delay: 50,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.1 
});

// HARDSKILLS
ScrollReveal().reveal('.hardskills', {
  origin: 'top',       // Aparece desde la izquierda
  distance: '50px',    // Se desplaza 100px
  duration: 1000,       // Dura 1 segundo
  delay: 100,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.3 
});

// PROJECTS
// APP
ScrollReveal().reveal('.app', {
  origin: 'left',       // Aparece desde la izquierda
  distance: '0px',    // Se desplaza 100px
  duration: 400,       // Dura 1 segundo
  delay: 150,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.3 
});

// DASHBOARD
ScrollReveal().reveal('.dashboard', {
  origin: 'left',       // Aparece desde la izquierda
  distance: '0px',    // Se desplaza 100px
  duration: 600,       // Dura 1 segundo
  delay: 350,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.3 
});

// SAMEL
ScrollReveal().reveal('.samel', {
  origin: 'left',       // Aparece desde la izquierda
  distance: '0px',    // Se desplaza 100px
  duration: 800,       // Dura 1 segundo
  delay: 550,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.3 
});

// MAREO
ScrollReveal().reveal('.mareo', {
  origin: 'left',       // Aparece desde la izquierda
  distance: '0px',    // Se desplaza 100px
  duration: 1000,       // Dura 1 segundo
  delay: 750,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.3 
});

// FOOTER
// SOCIAL
ScrollReveal().reveal('.social', {
  origin: 'left',       // Aparece desde la izquierda
  distance: '50px',    // Se desplaza 100px
  duration: 1000,       // Dura 1 segundo
  delay: 150,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.3 
});

// FORM CONTACT
ScrollReveal().reveal('.form-contact', {
  origin: 'top',       // Aparece desde la izquierda
  distance: '50px',    // Se desplaza 100px
  duration: 1000,       // Dura 1 segundo
  delay: 450,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.3 
});

// NAV BAR FOOT
ScrollReveal().reveal('.nav-bar-foot', {
  origin: 'left',       // Aparece desde la izquierda
  distance: '50px',    // Se desplaza 100px
  duration: 1000,       // Dura 1 segundo
  delay: 450,           // Retraso de 0.3 segundos
  scale: 0.50,          // Aparece "creciendo" desde el 85% de su tamaño
  opacity: 0,           // Comienza invisible
  reset: false,         // Solo se anima la primera vez
  easing: 'ease-in-out',  // Animación suave
  viewFactor: 0.3 
});





